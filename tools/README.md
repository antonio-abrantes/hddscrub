# tools

Optional portable smartmontools files can be placed here for local use:

```text
smartctl.exe
drivedb.h
```

These files are ignored by git because they are third-party binaries/data and can be reinstalled or recopied locally.
